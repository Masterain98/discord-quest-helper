## ✨ What's Changed

1. Added support for Linux (x86_64) desktop
  - Currently support only target: Ubuntu 26.04
  - Added automatic Discord login through the built-in browser connection on Linux (CDP)
  - Added a desktop shortcut (launcher) option on Linux for quicker access.
2. Fixed several cases where the app could pick the wrong game or get stuck after an error, especially right after launch.
3. Refreshed the Nitro badge icon.
4. Bumped dependencies to the latest patched versions.

